import styled from "styled-components";
import StyledKeypad from "./StyledKeypad";

const StyledKeypadRow = styled.div`
    &&& {
        display: flex;
        flex-direction: row;
    }  
`;

export default StyledKeypadRow